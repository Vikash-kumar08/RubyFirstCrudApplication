<br/>

<h>Showing specific user</h>

<p><strong>Username: </strong><%= @user.username %></p>
<p><strong>Email: </strong><%= @user.email %></p>

<%= link_to '<i class="glyphicon glyphicon-pencil"></i>'.html_safe, user_path(@user.id), class: 'btn btn-default' %> |
<%= link_to '<i class="glyphicon glyphicon-trash"></i>'.html_safe, user_path(@user.id), method: :delete, class: 'btn btn-default',data: {confirm: "Are you sure?"} %> |
<%= link_to 'Return to users listing', users_path %>
